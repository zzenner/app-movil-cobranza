package cl.zzenner.cobranza.usuarios.dominio;

/**
 * Constantes de código de rol para uso en código Java.
 * La fuente autoritativa es la tabla cobranza.roles; este enum facilita
 * referencias tipadas sin ser el origen de los datos.
 */
public enum CodigoRol {
    JEFE_SUPERVISORES, TECNOLOGIA, SUPERVISOR, EJECUTIVO_TERRENO
}
